package sept.ex160924;

public class Lab022_Interview_QnA {
    public static void main(String[] args) {
        //Q - Can enum be used as variable_name?
        //int enum = 10;
        //A - No,'enum' is a keyword so we cannot use it as variable name.
        //Since java is case-sensitive we can use Enum as variable name.
        int Enum =20;
        System.out.print(Enum);
    }
}
